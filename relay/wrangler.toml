name = "fractalize-relay"
main = "worker.js"
compatibility_date = "2025-01-01"

[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

# SQLite-backed Durable Objects (available on the Workers Free plan too).
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
